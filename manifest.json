{
	"id": "ff80cf94-d7c5-4a85-894c-26032864ae9e",
	"name": "2D Frame",
	"description": "Frame-animation viewer for exported 2D character sprite folders",
	"version": "0.1.1",
	"minAppVersion": "0.2.0",
	"permissions": {
		"sourceMeta": true,
		"searchMeta": false,
		"danmaku": false,
		"message": false
	},
	"ui": {
		"card": {
			"image": {
				"bl": [
					"{{if(source.character.clips, join(' ', join('', icon('play-circle'), number(source.character.clips)), if(gt(source.character.soundEvents, 0), join('', icon('music-notes'), number(source.character.soundEvents)))), if(gt(source.collection.characters, 0), join('', icon('clapperboard'), number(source.collection.characters))))}}"
				],
				"br": [
					"{{if(coverMeta.width, join('x', coverMeta.width, coverMeta.height))}}"
				]
			},
			"default": {
				"bl": [
					"{{if(source.character.clips, join(' ', join('', icon('play-circle'), number(source.character.clips)), if(gt(source.character.soundEvents, 0), join('', icon('music-notes'), number(source.character.soundEvents)))), if(gt(source.collection.characters, 0), join('', icon('clapperboard'), number(source.collection.characters))))}}"
				]
			}
		}
	},
	"i18n": {
		"name": {
			"en": "2D Frame",
			"zh": "2D 帧动画"
		},
		"description": {
			"en": "Frame-animation viewer for exported 2D character sprite folders",
			"zh": "播放自包含角色目录导出的 2D 精灵帧动画（含按帧触发的音频）"
		}
	}
}
